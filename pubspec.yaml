name: widgets_pack
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.4 
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

fonts:
  - family: Helvetica
    fonts:
      - asset: lib/src/utils/fonts/Helvetica.ttf